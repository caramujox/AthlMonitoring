import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:athl_monitoring/app/modules/home/pages/wrapper/wrapper_controller.dart';
import 'package:athl_monitoring/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // WrapperController wrapper;
  //
  setUp(() {
    //     wrapper = HomeModule.to.get<WrapperController>();
  });

  group('WrapperController Test', () {
    //   test("First Test", () {
    //     expect(wrapper, isInstanceOf<WrapperController>());
    //   });

    //   test("Set Value", () {
    //     expect(wrapper.value, equals(0));
    //     wrapper.increment();
    //     expect(wrapper.value, equals(1));
    //   });
  });
}
