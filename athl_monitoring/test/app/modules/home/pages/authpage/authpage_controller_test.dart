import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';


import 'package:athl_monitoring/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // AuthpageController authpage;
  //
  setUp(() {
    //     authpage = HomeModule.to.get<AuthpageController>();
  });

  group('AuthpageController Test', () {
    //   test("First Test", () {
    //     expect(authpage, isInstanceOf<AuthpageController>());
    //   });

    //   test("Set Value", () {
    //     expect(authpage.value, equals(0));
    //     authpage.increment();
    //     expect(authpage.value, equals(1));
    //   });
  });
}
