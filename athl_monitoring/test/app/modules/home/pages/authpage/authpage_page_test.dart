import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:athl_monitoring/app/modules/home/pages/authpage/authpage_page.dart';

void main() {
  testWidgets('AuthpagePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AuthpagePage(title: 'Authpage')));
    //  final titleFinder = find.text('Authpage');
    //  expect(titleFinder, findsOneWidget);
  });
}
